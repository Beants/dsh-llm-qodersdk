# The dsh-llm-qoder bundle patch: activates the Qoder provider adapter as a
# profile layer row over the profile root. The user's profile cordis.patch.yml
# may address this row by id.
- insert:
    - id: llm-qoder
      name: '@jiamingzang/dsh-llm-qoder'
